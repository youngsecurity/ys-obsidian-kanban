## 2.0.51+ys.5

- Position the pin immediately beside the card menu without reducing content width.
- Add a browser layout regression check for ordinary and filter-style cards at multiple widths.

- Add Pin to top and Unpin card controls with a visible pin indicator.
- Keep ordinary cards below pinned cards across insertion and movement paths.
- Allow pinned-card reordering; retain pins on moves and in archived Markdown; create unpinned card duplicates.
- Persist pins through Markdown edits and reloads while preserving multiline content and Tasks query fences.
- Add ordering, parsing, and upstream 2.0.51 round-trip regression tests.
